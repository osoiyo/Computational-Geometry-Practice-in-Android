<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@drawable/img_2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/img_2"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPaint"
        tools:ignore="MissingClass">

        <activity
            android:name=".activities.Paint3Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="Paint3" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Paint2Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="Paint2" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Paint1Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="Paint1" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>